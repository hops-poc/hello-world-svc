# hello-world-svc

Generated by `paved-road`'s scaffold. This repo's thinness is the product demo, not a
placeholder — the pipeline, policies, and IAM live in `paved-road`, not here. Keep it thin.

Live: one `infra/main.tf` for both `dev` and `prod`, config-driven via `infra/config.yaml`,
deployed through the real pipeline and served through CloudFront. Required status checks,
a repo ruleset, CODEOWNERS, and `dev`/`prod` GitHub Environments (prod requires a human
reviewer) are configured.

`.claude/skills/ship` and `.claude/skills/watch` drive console-first DX (PRD §11) —
`ship` pushes a change through gates → dev → human-approved prod, `watch` tails a run
and surfaces ReviewBot/Triage narration. Both are prompt-driven wrappers over `gh`,
not a separate `paved` CLI binary — see each SKILL.md.

No `.mcp.json` here — GitHub MCP is a dev-environment tool for the `hops.ai-demo` planning
workspace only, not something this generated repo ships with.
